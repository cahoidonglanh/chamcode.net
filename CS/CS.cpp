#include <bits/stdc++.h>
using namespace std;

int main()
{
    freopen("CS.inp", "r", stdin); freopen("CS.out", "w", stdout); string s; cin >> s; for(int i = 0; i < s.size(); i++){if(s[i] >= '0' && s[i] <='9') cout << s[i];}
    return 0;
}
