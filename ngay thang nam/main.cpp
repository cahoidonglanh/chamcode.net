#include <bits/stdc++.h>
using namespace std;

int main()
{
    int d, m, y;
    cin >> d >> m >> y;
    if(d <= 9, m <= 9)
        cout << 0 << d << "/" << 0 << m << "/" << y;
    else
        cout << d << "/" << m << "/" << y;
    return 0;
}
